<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.papreeca.app" >

    <!-- Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- Splash Screen -->
        <activity
            android:name="com.example.papreeca.app.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Screen -->
        <activity
            android:name="com.example.papreeca.app.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Intro Screen -->
        <activity
            android:name="com.example.papreeca.app.IntroActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_intro"
            android:theme="@android:style/Theme.NoTitleBar">
        </activity>

        <!-- Battle Screen -->
        <activity
            android:name="com.example.papreeca.app.BattleActivity"
            android:label="@string/title_activity_battle" >
        </activity>

        <!-- Battle Result Screen -->
        <activity
            android:name="com.example.papreeca.app.BattleResult"
            android:label="@string/title_activity_battle_result" >
        </activity>

        <!-- Join Screen -->
        <activity
            android:name="com.example.papreeca.app.JoinActivity"
            android:label="@string/title_activity_join" >
        </activity>
    </application>

</manifest>
